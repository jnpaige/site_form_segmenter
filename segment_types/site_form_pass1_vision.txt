You are analyzing a contact sheet image showing all pages of a Louisiana archaeological site form PDF.

WHAT YOU ARE SEEING:
A single image containing thumbnail versions of every page, arranged in a grid (left-to-right, then top-to-bottom). Each thumbnail is labeled with its 0-indexed page number (e.g. "p0", "p1", "p2") in red text at the top-left corner.

DOCUMENT STRUCTURE:
A typical PDF contains one or more complete site forms for one archaeological site appended back-to-back. Each site form documents one investigation and contains some combination of:
- Site record form page — a structured data-entry form with labeled boxes and fields (Site Number, Cultural Affiliation, Survey Methods, etc.)
- USGS quad maps, aerial photographs, site sketch maps
- Photograph pages
- Narrative pages with paragraphs of prose text
- LACAD coding forms or other supplemental structured forms

VISUAL CLUES that a new investigation's site form starts:
- A new structured form layout appears — labeled boxes, checkboxes, field headers like "SITE RECORD FORM", "UPDATE", or "LOUISIANA ARCHAEOLOGICAL SITE FILE"
- The visual layout shifts from dense prose or maps back to a form-style grid
- A new project date, author block, or agency logo appears at the top of a page
- A page with a fresh form header that restarts the field sequence

YOUR TASK:
Identify each distinct site investigation (site form) in this document. For each, return the contiguous range of page numbers that belong to it. Use the red page number labels (p0, p1, p2...) to identify specific pages.

Return ONLY a JSON array — no explanation, no markdown:
[{"label": "short label e.g. '2016 Phase I Survey'", "year": integer or null, "pages": [list of all integer page numbers in this investigation]}]
